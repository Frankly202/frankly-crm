# FranklyEdu Global CRM — Frontend Environment Configuration
# Override backend API URL if running on a non-default host/port (defaults to http://localhost:4000/api/v1)
VITE_API_BASE_URL=http://localhost:4000/api/v1
